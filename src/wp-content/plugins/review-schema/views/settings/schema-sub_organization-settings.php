<?php

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Settings for sub_organization Info
 */
$options = array( );

return apply_filters( 'rtrs_schema_sub_organization_settings_options', $options );
